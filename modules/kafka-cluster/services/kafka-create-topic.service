

[Unit]
Description=Criar topicos
After=network.target kafka_server.service

[Service]
Type=oneshot
ExecStart=sudo ${kafka_home}/bin/kafka-topics.sh --bootstrap-server ${join(",", bootstrap_servers)} --create --topic ${topics}

[Install]
WantedBy=multi-user.target